{
  "buildCommand": "cd client && npm run build",
  "outputDirectory": "client/.next",
  "installCommand": "cd shared && npm install && npm run build && cd ../client && npm install",
  "framework": "nextjs"
}
